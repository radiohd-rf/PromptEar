# Баг-репорты

## PromptEar v0.11.2

| ID | Описание | Шаги | Фактический результат | Ожидаемый результат | Приоритет | Статус |
|----|----------|------|----------------------|----------------------|-----------|--------|
| 1 | DOCX не создаётся в output/ для видео | 1. Загрузить MP4 файл. 2. Нажать Обработать. 3. Дождаться завершения. 4. Открыть папку output/. | Файл не создаётся или создаётся с ошибкой. В логе ошибка сохранения. | После обработки видео в output/ появляется .docx файл с транскрипцией. | High | Fixed |
| 2 | Загруженные файлы не удаляются после обработки | 1. Загрузить файлы (mp3 + mp4). 2. Нажать Обработать. 3. Проверить папку приложения после завершения. | Файлы остаются лежать в папке приложения, засоряя пространство. | После обработки оригинальные загруженные файлы удаляются автоматически. | Medium | Fixed |
| 3 | Двойной скроллбар в окне приложения | 1. Открыть приложение. 2. Загрузить 10+ файлов. 3. Прокрутить список. | Появляется два скроллбара — один на body, второй на списке файлов. Невозможно понять какой работает. | Единый скроллбар только на списке файлов, body overflow: hidden. | Low | Fixed |
| 4 | Дроп файла вне drop-zone открывает файл в браузере | 1. Перетащить файл за пределы drop-zone (на серый фон). 2. Отпустить. | Браузер/WebView открывает файл в новой вкладке. Интерфейс приложения сбрасывается. | Дроп вне drop-zone игнорируется, ничего не происходит. | High | Fixed |
| 5 | Дублирующийся Escape-слушатель | 1. Открыть модалку help. 2. Нажать Escape 2-3 раза. | Модалка закрывается, затем снова открывается. Escape-обработчик вызывает коллизию с другими листенерами. | Один обработчик Escape, модалка только закрывается. | Medium | Fixed |
| 6 | setStatus() не определён при отмене | 1. Нажать Обработать. 2. Сразу нажать Отмена. | Ошибка JS: "setStatus is not defined". Консоль красная, UI в поломанном состоянии (кнопки не восстанавливаются). | Обработка отменяется, UI корректно восстанавливается без ошибок JS. | Critical | Fixed |
| 7 | Cancel не проброшен в pipeline-компоненты | 1. Начать обработку. 2. Нажать Отмена. 3. Проверить логи. | Транскрибация/enhance продолжаются после отмены. cancel event не передаётся в Whisper, Qwen, ffmpeg. | Отмена мгновенно останавливает все компоненты (Whisper, Qwen, ffmpeg). | Critical | Fixed |
| 8 | Версия в build_zips.py: v0.11.0 вместо v0.11.2 | 1. Запустить python build_zips.py. 2. Проверить имя выходного zip. | Имя файла PromptEar-v0.11.0-{variant}.zip — старая версия. | Имя файла PromptEar-v0.11.2-{variant}.zip — актуальная версия. | Medium | Fixed |
| 9 | Compress-Archive не поддерживает параметр -Exclude | 1. В build_zips.py используется Compress-Archive -Exclude. 2. Запустить сборку. | Ошибка PowerShell: "Compress-Archive: A parameter cannot be found that matches parameter name 'Exclude'". | Сборка проходит без ошибок. Файлы корректно исключаются через Where-Object. | High | Fixed |
| 10 | pip download не хватает места на C: | 1. Запустить build_zips.py на системе с малым местом на C:. | Сборка падает с ошибкой "No space left on device" на C:. | TEMP переносится на G: (где есть место), сборка проходит успешно. | High | Fixed |
| 11 | Длинные пути в venv — папка _deleteme не удаляется | 1. Скопировать или создать папку с длинными путями (>260 символов). 2. Попытаться удалить через Проводник / cmd rmdir. | Ошибка: "Source path too long". Папка не удаляется стандартными средствами. | Папка удаляется (через robocopy /MIR или \\?\ префикс). | Low | Fixed |
| 12 | .pyd/.dll заблокированы Windows Defender при удалении | 1. Попытаться удалить папку с CUDA-зависимостями (torch .dll/.pyd). 2. Использовать rmdir /s, robocopy /MIR, del /f. | "Access denied" на всех .dll и .pyd файлах. Файлы не удаляются даже с правами администратора. | Все файлы удаляются без блокировок. | Low | Known Issue (требует перезагрузки) |
| 13 | Ссылки в README вели на неактуальную версию и старый Яндекс.Диск | 1. Открыть README.md. 2. Проверить ссылки на релизы и Яндекс.Диск. | CPU ссылка ведёт на v0.11.0. Яндекс.Диск ссылка ведёт на старый URL. | Ссылки ведут на актуальную версию v0.11.2 и правильный Яндекс.Диск. | Medium | Fixed |
| 14 | Потеря данных при перезапуске во время обработки | 1. Запустить обработку нескольких файлов. 2. Закрыть приложение до завершения. 3. Открыть снова. | Уже обработанные файлы сохранились, но частично обработанные — нет. Нет индикации что было прервано. | При повторном открытии пользователь видит какие файлы уже обработаны, какие нет. | Critical | Open |
| 15 | Ошибки таймаутов при длительной обработке Ollama | 1. Загрузить файл > 1 ГБ. 2. Начать обработку. 3. Дождаться шага Enhance. | Ollama возвращает timeout после 300 сек. Файл не обработан, pipeline падает. | Таймаут увеличен или реализована повторная попытка (retry). При превышении — fallback с сохранением уже полученного текста. | Critical | Open |
| 16 | Видео без аудиодорожки — неинформативная ошибка | 1. Загрузить видео без аудио (например, screen record без звука). 2. Нажать Обработать. | Ошибка "Не удалось прочитать видео" без уточнения причины. | Ошибка: "В видео ... нет аудиодорожки". Пользователь понимает причину. | Medium | Fixed |
| 17 | Лаунчер не убивает python.exe при закрытии | 1. Запустить приложение через Запустить PromptEar.exe. 2. Закрыть окно. 3. Проверить Task Manager. | python.exe остаётся в процессах, висит бесконечно. | При закрытии окна Job Object убивает python.exe. | High | Fixed |
| 18 | Safety ratio reject без уведомления пользователя | 1. Обработать файл с очень короткой речью. 2. Проверить результат. | Enhancement rejected result < 60% от оригинала, но пользователю не показывается что enhancer сбросил результат. Транскрипция без улучшения. | В логе пишется "⚠ Улучшение отклонено (result too short), сохранена предыдущая версия". | Low | Fixed |
| 19 | Нет иконки приложения в заголовке окна и таскбаре | 1. Запустить приложение. 2. Посмотреть на заголовок окна и иконку в панели задач. | Вместо иконки PromptEar отображается пустая/дефолтная иконка Python. Launcher пытается загрузить "MAINICON" из своих ресурсов (launcher.cs:209) — но ресурса нет. PyWebView create_window() тоже не получает icon. | В заголовке окна и таскбаре отображается кастомная иконка PromptEar (assets/icon_titlebar.ico). | Medium | Open |
| 20 | Сборка v0.12.0 — отсутствуют wheels для flask и зависимостей | 1. Распаковать PromptEar-v0.12.0-cpu.zip. 2. Запустить приложение. 3. Дождаться bootstrap. | При запуске: ModuleNotFoundError: No module named 'flask'. pip скачивает зависимости из интернета — если нет сети, приложение не работает. | В wheels/ лежат все зависимости (flask, pywebview, faster-whisper, requests, python-docx, Pillow). bootstrap устанавливает из wheels без доступа в интернет. | Critical | Open |
